//! Convenience prelude — `use aaf_memory::prelude::*;` brings the
//! most-used types into scope.

pub use crate::context::{ContextBudget, ContextSection, DEFAULT_TOTAL_BUDGET};
pub use crate::facade::MemoryFacade;
